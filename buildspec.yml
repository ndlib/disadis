---
version: 0.2


phases:
  install:
    commands:
      - apt-get install ruby ruby-dev ruby-gems build-essential
      - gem install --no-ri --no-rdoc fpm

  build:
    commands:
      - mkdir -p /go/src/github.com/ndlib
      - ln -s / /go/src/github.com/ndlib/disadis
      - export PATH="/go/bin:/usr/local/go/bin:$PATH"
      - make rpm


artifacts:
  files:
    - disadis-*rpm
...
